Context: CareSync, a React + TypeScript + Tailwind v3 + React Router app following my Figma. Plain styling for now (no theme or reusable components yet). Routes: /login, /dashboard, /care-plan, /wellbeing, /sync. Shared state in src/context/AppContext.tsx (memory only, no storage): isLoggedIn, checkedInAt, tasks, vitals, notes, wellbeingType, syncStatus, syncSource, syncedAt, simulateFailure, with triggerSync and resetSync. Sync screen shows shift-completed or wellbeing-request confirmations depending on syncSource, and a failure state with a demo toggle. Everything currently works.